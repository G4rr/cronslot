{
    "name": "cronslot",
    "description": "read and visualize crontab file",
    "scripts": {
        "dev": "vite",               // or "react-scripts start" if using CRA
        "build": "vite build",       // or "react-scripts build"
        "preview": "vite preview"    // or remove if CRA
    }
}
